{
  "devDependencies": {
    "concurrently": "^9.2.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "start": "npm-run-all --parallel server client",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev"
  }
}
